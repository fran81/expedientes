ngDescribe({
    name: 'Test cuenta-cargar component',
    modules: 'app',
    element: '<cuenta-cargar></cuenta-cargar>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
